<h1>Listing user_follows</h1>

<table>
  <tr>
    <th> user</th>
    <th>Foolwer</th>
    <th>Following date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_follows.each do |user_follow| %>
  <tr>
    <td><%= user_follow. user_id %></td>
    <td><%= user_follow.foolwer_id %></td>
    <td><%= user_follow.following_date %></td>
    <td><%= link_to 'Show', user_follow %></td>
    <td><%= link_to 'Edit', edit_user_follow_path(user_follow) %></td>
    <td><%= link_to 'Destroy', user_follow, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User follow', new_user_follow_path %>
