<%= form_for(@user_follow) do |f| %>
  <% if @user_follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_follow.errors.count, "error") %> prohibited this user_follow from being saved:</h2>

      <ul>
      <% @user_follow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :foolwer_id %><br />
    <%= f.text_field :foolwer_id %>
  </div>
  <div class="field">
    <%= f.label :following_date %><br />
    <%= f.date_select :following_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
